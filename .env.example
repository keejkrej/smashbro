# Optional locally. Required in production so two players on different
# Vercel Function instances can find the same room.
#
# Add Upstash Redis from the Vercel Marketplace, then:
#   vercel env pull .env.local
REDIS_URL=
